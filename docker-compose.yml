services:
  evidence-appraisal-tool:
    build: .
    ports:
      - "10000:10000"
    environment:
      NODE_ENV: production
      PORT: 10000
      EVIDENCE_DATA_DIR: /app/data
    volumes:
      - evidence_data:/app/data
    restart: unless-stopped

volumes:
  evidence_data:
