import React from 'react';import {getHelpEntry} from '../../services/helpRegistryService';export default function HelpTooltipModal({topic,onClose}:{topic:string;onClose:()=>void}){const h=getHelpEntry(topic);return <div role="dialog" aria-modal="true"><button onClick={onClose}>Lukk</button><h2>{h.title}</h2><p>{h.definition}</p><p><strong>Tommelfingerregel:</strong> {h.ruleOfThumb}</p><p><strong>Referanse:</strong> {h.reference}</p><ol>{h.steps.map(s=><li key={s}>{s}</li>)}</ol><svg width="320" height="40" aria-label="Micro-tutorial"><circle cx="20" cy="20" r="8"><animate attributeName="cx" from="20" to="300" dur="2s" repeatCount="indefinite"/></circle></svg></div>}