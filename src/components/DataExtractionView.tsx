import React from 'react';import {validateExtraction,type ExtractionMatrix} from '../services/dataExtractionService';export default function DataExtractionView({matrix}:{matrix:ExtractionMatrix}){const v=validateExtraction(matrix);return <section><h2>Data Extraction & Synthese</h2><p>Utvalg: {matrix.nTotal} totalt ({matrix.nIntervention} intervensjon, {matrix.nControl} kontroll).</p><p>Status: {v.valid?'Validert':'Krever korrigering'}</p>{[...v.errors,...v.warnings].map(x=><p key={x}>{x}</p>)}</section>}

