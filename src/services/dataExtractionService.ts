export interface QuantitativeOutcome{ id:string;name:string;timepoint?:string;measure:'MEAN_DIFFERENCE'|'STANDARDIZED_MEAN_DIFFERENCE'|'RR'|'OR';meanIntervention?:number;sdIntervention?:number;meanControl?:number;sdControl?:number;eventsIntervention?:number;eventsControl?:number;ciLow?:number;ciHigh?:number;pValue?:number; }
export interface ExtractionMatrix{studyId:string;nTotal:number;nIntervention:number;nControl:number;baselineCharacteristics:Record<string,string>;intervention:string;comparator:string;primaryOutcomes:QuantitativeOutcome[];secondaryOutcomes:QuantitativeOutcome[];qualitative:{phenomenon:string;context:string;theoreticalFramework:string;themes:string[];quotes:Array<{text:string;location:string;researcherVerified:boolean}>};}
export interface ExtractionValidation{valid:boolean;errors:string[];warnings:string[];}
export function validateExtraction(m:ExtractionMatrix):ExtractionValidation{const errors:string[]=[];const warnings:string[]=[];if(m.nTotal!==m.nIntervention+m.nControl)errors.push('N total mÃ¥ vÃ¦re lik intervensjon + kontroll.');if(m.nTotal<=0)errors.push('N total mÃ¥ vÃ¦re positiv.');for(const o of [...m.primaryOutcomes,...m.secondaryOutcomes]){if(o.ciLow!==undefined&&o.ciHigh!==undefined&&o.ciLow>o.ciHigh)errors.push(`Konfidensintervallet er inkonsistent for ${o.name}.`);if(o.measure.startsWith('MEAN')&&(o.meanIntervention===undefined||o.meanControl===undefined))warnings.push(`Manglende gjennomsnittsdata for ${o.name}.`);}if(!m.primaryOutcomes.length)warnings.push('Ingen primÃ¦re utfall er registrert.');return{valid:errors.length===0,errors,warnings};}
