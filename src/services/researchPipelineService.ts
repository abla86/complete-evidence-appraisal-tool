import { generateId } from '../utils/id';
/**  * Shared research-pipeline contracts and deterministic helpers.  *  * This is intentionally orchestration-only: it does not replace JBI/CASP/  * AMSTAR/AGREE/RoB/GRADE engines. It keeps the superprogram extensible by  * expressing the links between modules without collapsing their semantics.  */  export type PipelineStage =   | 'SEARCH'   | 'IMPORT'   | 'REFERENCE_HUB'   | 'DUPLICATE_REVIEW'   | 'FULLTEXT'   | 'SOURCE_RECORD'   | 'SCREENING'   | 'PICO'   | 'APPRAISAL'   | 'DUAL_REVIEW'   | 'CONSENSUS'   | 'EXTRACTION'   | 'SYNTHESIS'   | 'CERTAINTY'   | 'PRISMA'   | 'WRITING'   | 'EXPORT';  export interface PipelineLink {   id: string;   from: PipelineStage;   to: PipelineStage;   recordIds: string[];   createdAt: string;   actor: string;   rationale?: string; }  export interface PipelineSnapshot {   currentStage: PipelineStage;   links: PipelineLink[];   blocked: boolean;   blockingReasons: string[]; }  export const DEFAULT_PIPELINE: PipelineStage[] = [   'SEARCH', 'IMPORT', 'REFERENCE_HUB', 'DUPLICATE_REVIEW', 'FULLTEXT',   'SOURCE_RECORD', 'SCREENING', 'PICO', 'APPRAISAL', 'DUAL_REVIEW',   'CONSENSUS', 'EXTRACTION', 'SYNTHESIS', 'CERTAINTY', 'PRISMA', 'WRITING', 'EXPORT' ];  export function createPipelineLink(   from: PipelineStage,   to: PipelineStage,   recordIds: string[],   actor: string,   rationale?: string, ): PipelineLink {   return {     id: `link-${generateId()}`,     from,     to,     recordIds: [...recordIds],     createdAt: new Date().toISOString(),     actor,     rationale,   }; }  export function buildPipelineSnapshot(   currentStage: PipelineStage,   links: PipelineLink[],   blockingReasons: string[] = [], ): PipelineSnapshot {   return {     currentStage,     links: [...links],     blocked: blockingReasons.length > 0,     blockingReasons: [...blockingReasons],   }; }  export function nextPipelineStage(stage: PipelineStage): PipelineStage | null {   const index = DEFAULT_PIPELINE.indexOf(stage);   return index >= 0 && index < DEFAULT_PIPELINE.length - 1 ? DEFAULT_PIPELINE[index + 1] : null; }   export interface PipelineAdvanceResult {   ok: boolean;   snapshot: PipelineSnapshot;   reason?: string; }  const STAGE_ORDER = new Map(DEFAULT_PIPELINE.map((stage, index) => [stage, index]));  export function validatePipelineTransition(from: PipelineStage, to: PipelineStage): boolean {   const fromIndex = STAGE_ORDER.get(from);   const toIndex = STAGE_ORDER.get(to);   return fromIndex !== undefined && toIndex !== undefined && toIndex === fromIndex + 1; }  export function advancePipeline(   snapshot: PipelineSnapshot,   to: PipelineStage,   actor: string,   recordIds: string[] = [],   rationale?: string, ): PipelineAdvanceResult {   if (snapshot.blocked) {     return { ok: false, snapshot, reason: snapshot.blockingReasons.join(' ') || 'Pipeline is blocked.' };   }   if (!actor.trim()) return { ok: false, snapshot, reason: 'actor is required.' };   if (!validatePipelineTransition(snapshot.currentStage, to)) {     return { ok: false, snapshot, reason: `Invalid pipeline transition: ${snapshot.currentStage} -> ${to}.` };   }   const link = createPipelineLink(snapshot.currentStage, to, recordIds, actor, rationale);   return { ok: true, snapshot: buildPipelineSnapshot(to, [...snapshot.links, link]) }; }
